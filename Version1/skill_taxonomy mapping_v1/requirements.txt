# ── Core ──────────────────────────────────────────────────────────────
streamlit>=1.30.0
pandas>=2.0.0
openpyxl>=3.1.0

# ── LLM Providers  (install all; only the one you use needs a key) ────
anthropic>=0.40.0
openai>=1.30.0
google-generativeai>=0.6.0
