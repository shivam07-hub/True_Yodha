# Browser-safe values only. NEXT_PUBLIC_* is bundled into the browser.
# A production build refuses to start when critical values are absent,
# placeholders are present, or service URLs do not use HTTPS.
# The Supabase anon key is safe here only because every exposed table/storage
# object is protected by RLS and the service-role key stays server-side.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
# Point this at the DEV backend (https://truemirror.up.railway.app) unless you
# are actually running uvicorn locally. Setting it to http://localhost:8000 with
# no backend up makes login fail with a bare "Network request failed" and no
# hint that the API is simply absent — it cost a full debugging session once.
# `npm run qa:mobile` preflights this and tells you before it wastes your time.
NEXT_PUBLIC_API_URL=https://your-backend.example.com
NEXT_PUBLIC_API_BASE_URL=https://your-backend.example.com

# Dedicated seeded QA account for `npm run qa:mobile`. NOT browser-exposed (no
# NEXT_PUBLIC_ prefix) — the capture harness reads them server-side only.
MYRO_TEST_EMAIL=qa@example.com
MYRO_TEST_PASSWORD=your-qa-account-password
NEXT_PUBLIC_RAZORPAY_KEY_ID=your-razorpay-public-key-id
# Optional early-stage anti-bot layer. Keep false unless the matching backend
# flag and both Cloudflare keys are intentionally provisioned.
NEXT_PUBLIC_TURNSTILE_ENABLED=false
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key
# Optional canonical override. Public-profile links fall back to the Vercel
# deployment URL and then https://himyro.com.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_APP_VERSION=
NEXT_PUBLIC_SEEKERS_COUNT=

# Server-only Next.js values used by server components/routes. API_INTERNAL_URL
# is optional and falls back to the public API URL. Do not rename these with
# NEXT_PUBLIC_ and do not put credentials in them.
API_INTERNAL_URL=https://your-backend.example.com
