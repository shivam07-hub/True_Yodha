/** The Myro Chrome extension — one listing URL, imported by every surface that
 *  offers it. Two surfaces already pointed at this ID from separate literals;
 *  a store URL that only half the app knows about is how a moved listing turns
 *  into a dead link on the page nobody re-checks. */
export const EXTENSION_WEBSTORE_URL =
  "https://chromewebstore.google.com/detail/paofacokffpipcpljelckndgghkddoph"
