# ── Web framework ──────────────────────────────────────────────
fastapi==0.110.3
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # file uploads

# ── Database ────────────────────────────────────────────────────
# SQLAlchemy/asyncpg/alembic removed — Supabase client handles all DB ops.
# Schema lives in database/schema.sql, applied via Supabase SQL Editor.

# ── Config & validation ─────────────────────────────────────────
pydantic[email]==2.7.1
pydantic-settings==2.2.1

# ── Auth ────────────────────────────────────────────────────────
supabase==2.4.3                  # Supabase Python client
python-jose[cryptography]==3.3.0 # JWT handling
passlib[bcrypt]==1.7.4           # password hashing

# ── CV parsing ──────────────────────────────────────────────────
openpyxl==3.1.2                  # read xlsx files for csv_importer
python-dotenv==1.0.1             # load .env in standalone scripts
pymupdf>=1.24.2                  # PDF text extraction (no strict pin — ARM64 wheel availability varies)
python-docx==1.1.0               # DOCX text extraction

# ── Skill tagging (NLP) ─────────────────────────────────────────
spacy>=3.8.0                     # NLP pipeline for skill NER — needs >=3.8.0 for Python 3.13 support
# After pip install, run: python -m spacy download en_core_web_sm

# ── LLM (job re-ranking) ────────────────────────────────────────
openai==1.25.0                   # GPT-4o mini for job ranking
groq==0.9.0                      # Groq API client (primary LLM — skill tagging + ranking)

# ── HTTP client ─────────────────────────────────────────────────
httpx==0.27.0                    # async HTTP (used internally)

# ── Async job queue ─────────────────────────────────────────────
redis==5.0.7                     # Redis client for queue + status storage
rq==1.16.2                       # durable background worker queue

# ── Email ───────────────────────────────────────────────────────
sendgrid==6.11.0

# ── Testing ─────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6

# ── Linting ─────────────────────────────────────────────────────
ruff==0.4.4

# ── PDF generation ──────────────────────────────────────────────
reportlab>=4.2.0                 # CV PDF export

# ── Internal tools ──────────────────────────────────────────────
streamlit==1.35.0                # tagger_ui.py — browser-based HITL tagger
