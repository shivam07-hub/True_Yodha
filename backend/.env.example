SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# LLM — fallback order: groq → gemini → openrouter
# Add all three for maximum coverage. Each is free tier.
GROQ_API_KEY=
GOOGLE_API_KEY=          # Gemini 1.5 Flash — console.cloud.google.com
OPENROUTER_API_KEY=      # openrouter.ai — free account, no daily cap

# Job re-ranking (Phase 1F)
OPENAI_API_KEY=

# Email (post-MVP)
SENDGRID_API_KEY=

# Deployment
RAILWAY_ENVIRONMENT=development
ALLOWED_ORIGINS=["http://localhost:3000"]
