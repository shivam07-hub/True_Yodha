{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python -m app.workers.job_listing_verifier",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
