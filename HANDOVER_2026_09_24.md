# Handover — 2026-09-24

One session, started from a single question: *"Shraddha Gupta uploaded her CV —
find her the jobs she can apply to, and tell me how her onboarding went."*
She had **zero matches**. Everything below came out of answering why.

Root the work in [BACKLOG.md](BACKLOG.md); this file only says what changed,
what is unverified, and what is dangerous to assume. **Nothing here is a
substitute for measuring again** — several entries in this session existed to
correct an earlier confident note.

---

## Shipped (all on `Develop`, all six gates green)

| Commit | What |
|---|---|
| `b5275528` | Direction emits `succeeded` / `failed` — the last onboarding step had 45 `started` rows and no terminal outcome, ever |
| `63f86108` | `seniority_fit` — one reading of a job's level for both the gate and the verdict |
| `ce6e9694` | Ingestion dead-man belt + the Notice digest only speaks when something moved |
| `6e62502c` | One admission rule — browse admits an unreadable level too |
| `ccf43777` | Telemetry: a dropped beacon says so; TS ↔ Python ↔ SQL vocabularies tied together |
| `82724e84` | `route.latency` bucket counts + the instrument audit (§19) |
| `d454a109` | `RoutePerfProbe` — real-user latency, rebuilt from a pipe that never worked |
| `f2ca340b` | Backlog: the 15s ceiling, and four open items filed into existing entries |

---

## The three things most likely to be assumed wrong

**1. `/health` reporting `ingestion: stalled` is CORRECT, not a bug in the new
belt.** Job ingestion genuinely stopped on 2026-09-09: 1 job in 9 days, 2,715
retired in 6, while the verifier stayed healthy and kept retiring listings from
a corpus nothing refilled. Verify before doubting it:

```sql
select max(started_at) from job_source_runs;
```

The root cause is in the **scraper repo**, `CLAUDE.md → PENDING WORK → 00`
(four runs since the 9th each die right after planning 319 portals, no error;
it runs from a local Codex automation on Shivam's laptop). ⚠️ **That file is
UNCOMMITTED** — it is lost if that tree is reset.

**2. An unreadable `seniority_level` is admitted on EVERY path, browse
included — and it must not be re-split.** This reversed mid-session: it shipped
pool-only, then Shivam flipped it once the Match Quality gate measured browse
hiding 9,323 listings. A flag there means two admission rules for one job. The
cost was knowingly accepted: an entry-level feed roughly triples.

**3. `p95 = 14,993ms` is censored, not measured.** It is
`frontend/lib/api.ts:63`, `REQUEST_TIMEOUT_MS = 15_000` — status 499, the
browser aborting. The real tail is above it and invisible. A second ceiling at
exactly 30,000ms is **not identified**; find it before raising the 15s default,
or the raise just moves the wall.

---

## Unverified — do not report these as working

- **Both new latency instruments have produced no live data yet.** After the
  next deploy: `railway logs | grep "metric route.latency"` and
  `select count(*) from route_perf_events`. If either stays empty, the rebuild
  failed the same way the original did (ARCHITECTURE_READ_PATH §19.2).
- **Direction's new telemetry has not seen a real user.** It proves itself the
  first time someone finishes the step:
  `select outcome, count(*) from cv_upload_phase_events where phase='direction' group by 1;`
  Until a `succeeded` or `failed` row exists, we still cannot say why 5 of 27
  people left.
- **The ingestion belt has never fired its Notice**, because `stalled` opened
  immediately and no run has landed to close it.

---

## Open work, with its owner

Detail lives in BACKLOG; this is the map.

| Work | Filed at | Note |
|---|---|---|
| Direction opens answered (tap ×5) | **#46 → S6** | design fully locked with Shivam, not built |
| CV short-token false skills | CV parsing section | 69 of 403 users; `.NET` from "net", PHP→RPL |
| `signed-url` 221 unaccounted starts | **#16 → 3d** | same hole `direction` had |
| `/users/me` p95 2.7–4.2s | **#16 → 3d** | on every authed page; next latency target |
| The 30,000ms ceiling | **#16 → 3d** | unidentified |
| Ingestion root cause | scraper repo | Shivam's laptop |

⚠️ **Two of these sit in files another agent was actively working in** —
`frontend/components/onboarding/target-confirm.tsx` (they landed `5d31bf83`
mid-session, adding `withRealPrimary` and removing the `no_families` reason
code) and `backend/app/services/cv_parser.py` + friends (`79908258`).
**Re-measure before building** — part of the floor may already be there.

---

## Shraddha (`fc2f8f61-a357-434a-91c6-bcd90c6de6a3`)

Still has **no target and zero matches**, deliberately: the forward-pass rule
says she is brought forward when she next arrives, not migrated while away.
Her shortlist was built by hand and is at
`User job matches and CVs/shraddha_gupta_20260918/`. ⚠️ It is scoped to
**Delhi NCR** on an early instruction; Shivam later said she will relocate
anywhere in India, which moves the strongest matches to Bengaluru. Regenerate
before sending.

She is also the control case in another agent's `6b56dc47`: *"none of the 35 a
hand search found for her"* — which is how the `first_seen` defect (34,022 of
38,824 listings sharing one date, so `limit(CAP)` hands everyone the same
arbitrary slice) got measured.
