{
  "name": "myro-extension",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "package": "npm run build && cd dist && zip -qr ../myro-extension.zip ."
  }
}
